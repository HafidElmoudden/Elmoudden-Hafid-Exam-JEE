package ma.hafidelmoudden.backend.services;

public interface TemplateService {
    // Define the methods that will be implemented in the service class
    // For example:
    // List<TemplateDTO> getAllTemplates();
    // TemplateDTO getTemplateById(Long id);
    // void createTemplate(TemplateDTO templateDTO);
    // void updateTemplate(Long id, TemplateDTO templateDTO);
    // void deleteTemplate(Long id);
}
